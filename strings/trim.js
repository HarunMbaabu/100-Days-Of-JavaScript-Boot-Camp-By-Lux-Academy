/* remove all leading and trailing whitespace characters of a string, you use the trim() method. */

let rawString = ' Hi  ';
let strippedString = rawString.trim();
console.log(strippedString); // "Hi"
